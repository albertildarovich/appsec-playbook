# Модуль 5: Authentication

> **Цель:** Проверить механизмы аутентификации в Juice Shop
> **Формат:** разбираем каждый пункт по очереди

---

## План проверки

- [x] Регистрация — [NO] Mass Assignment, [NO] нет верификации email, [NO] нет капчи
- [x] Логин — [NO] нет rate limiting, [NO] нет блокировки
- [x] Logout — [NO] JWT не инвалидируется, токен живёт вечно
- [x] Смена пароля — нет UI/profile для смены пароля
- [x] Reset password — UI есть, но сломан (нет email-провайдера)
- [x] MFA — отсутствует (totpSecret в JWT есть, но не используется)

---

## Регистрация

**Что проверили:**
- POST `/api/Users` с несуществующим email — создан пользователь `id:26`
- Никакого подтверждения email не требуется
- Никакой капчи/anti-bot нет
- Можно передать `role: admin` (Mass Assignment — подтверждено в Модуле 4)

**Результат:** 
- [NO] Нет верификации email
- [NO] Нет anti-bot защиты
- [NO] Нет фильтрации полей (Mass Assignment)
- [OK] Можно создать пользователя — но безопасность регистрации отсутствует

**Риск:** High

---

## Логин

**Что проверили:**
- Rate limiting — отсутствует (проверено в Модуле 4 — 3 запроса подряд без блокировки)
- Блокировка аккаунта — отсутствует (можно брутфорсить бесконечно)
- Сообщение об ошибке — не раскрывает, email или пароль неверны [OK] (хорошая практика)
- JWT без Exp — уже нашли в Модуле 4

**Результат:**
- [NO] Нет rate limiting
- [NO] Нет блокировки аккаунта
- [OK] Ошибка не раскрывает sensitive info
- [NO] JWT бессрочный

**Риск:** Critical (bruteforce без ограничений)

---

## Logout

**Что проверили:**
- В браузере: JWT удаляется из localStorage ([OK] клиентская очистка работает)
- curl GET `/rest/user/logout` → **HTTP 500** (серверный endpoint не работает)
- Тот же JWT после logout → HTTP 200 на `/api/Users` (токен жив)

**Практическое подтверждение:**
Ты разлогинился в браузере, я проверил твой старый JWT — HTTP 200, все 26 пользователей с хешами паролей. Токен не был инвалидирован.

**Результат:**
- [NO] JWT не инвалидируется на сервере — токен живёт вечно
- [NO] Нет blacklist'а токенов
- [OK] UI-очистка localStorage работает, но не защищает от атакующего с украденным токеном

**Риск:** Critical (нет server-side инвалидации сессии)

---

## Reset Password

**Что проверили:**
- Страница `/#/forgot-password` — существует, но сломана (поля задизейблены)
- curl POST `/rest/user/reset-password` → заблокирован (anti-fraud защита — только с localhost)
- В локальной сборке reset password не работает без настроенного email-провайдера

**Результат:**
- [NO] UI есть, но не работает — Security Misconfiguration
- [OK] Anti-fraud защита от внешних запросов (но это не поможет, если атакующий на том же хосте)
- [NO] Невозможно протестировать механизм безопасности reset password в локальной сборке

**Риск:** Medium (функционал отключён, но UI вводит в заблуждение)

---

## MFA

**Что проверили:**
- В приложении нет страницы настройки двухфакторной аутентификации
- В JWT payload есть поле `totpSecret` — предполагается поддержка TOTP, но UI отсутствует
- MFA не используется и не может быть включена

**Результат:**
- [NO] MFA отсутствует
- [NO] Даже при admin-доступе нет двухфакторной защиты
- [OK] В payload JWT есть поле `totpSecret` — возможно, задел на будущее

**Риск:** High (особенно в сочетании с отсутствием rate limiting и бессрочными JWT)
