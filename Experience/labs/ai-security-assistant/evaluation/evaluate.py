"""Evaluation runner.

Status: skeleton (Phase 0). Implemented in Phase 6.

Loads scenarios from `evaluation/dataset/`, runs them through the agent and
produces an EvaluationReport per category.
"""

from __future__ import annotations


def run_evaluation(dataset_dir: str = "evaluation/dataset") -> dict:
    """Run all scenarios and return per-category metrics."""
    raise NotImplementedError("Phase 6")
